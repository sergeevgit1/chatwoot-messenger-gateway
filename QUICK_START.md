# 🚀 Быстрый старт и настройка Chatwoot Connectors

Этот гид поможет вам запустить и настроить проект Chatwoot Connectors для интеграции с различными мессенджерами.

## 📋 Содержание

1. [🚀 Автоматическая настройка (рекомендуется)](#-автоматическая-настройка-рекомендуется) - умная настройка за 2 минуты
2. [Быстрый старт](#быстрый-start) - запуск проекта за 5 минут
3. [Подробная настройка](#подробная-настройка) - получение всех необходимых кредов
4. [Интерактивная настройка](#интерактивная-настройка) - использование веб-интерфейса
5. [Команды для разработки](#команды-для-разработки) - полезные npm команды
6. [Проверка работоспособности](#проверка-работоспособности) - как проверить, что всё работает
7. [Частые проблемы](#частые-проблемы) - решение типичных проблем

## 🚀 Автоматическая настройка (рекомендуется)

Новый умный скрипт настройки автоматически генерирует все необходимые параметры и настраивает интеграцию с Chatwoot.

### Предварительные требования

- Node.js 18+ и npm
- Аккаунт ВКонтакте с созданным сообществом
- Установленный Chatwoot (можно использовать [облачную версию](https://www.chatwoot.com/))

### Что генерируется автоматически vs что нужно ввести вручную

| Параметр | Автоматически | Вручную |
|----------|---------------|---------|
| VK_CALLBACK_ID | ✅ UUID v4 | - |
| VK_SECRET | ✅ Случайная строка (32 символа) | - |
| CHATWOOT_WEBHOOK_ID_VK | ✅ UUID v4 | - |
| VK_API_VERSION | ✅ 5.199 | - |
| HOST | ✅ 0.0.0.0 | - |
| VK_INBOX_ID | ✅ Создается через API | - |
| Chatwoot Base URL | - | ✅ URL вашего Chatwoot |
| Chatwoot API Token | - | ✅ Токен доступа |
| Chatwoot Account ID | - | ✅ ID аккаунта |
| VK Access Token | - | ✅ Токен сообщества |
| VK Group ID | - | ✅ ID сообщества |
| VK Confirmation String | - | ✅ Строка из настроек VK |

### Шаг 1: Установка проекта

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd chatwoot-messenger-gateway

# Установите зависимости
npm install
```

### Шаг 2: Автоматическая настройка

```bash
# Перейдите в директорию VK коннектора
cd apps/vk-connector

# Запустите автоматическую настройку
npm run auto-setup
```

Скрипт пошагово запросит только необходимые параметры:
1. **Chatwoot Base URL** - URL вашего Chatwoot сервера
2. **Chatwoot API Access Token** - токен доступа к API
3. **Chatwoot Account ID** - ID аккаунта
4. **VK Access Token** - токен доступа сообщества
5. **VK Group ID** - ID сообщества ВКонтакте
6. **VK Confirmation String** - строка подтверждения из настроек Callback API
7. **Server Port** - порт сервера (по умолчанию 3000)

### Шаг 3: Что делает скрипт автоматически

1. ✨ **Генерирует все недостающие параметры** (UUID, секретные ключи)
2. 🔍 **Проверяет подключение к Chatwoot**
3. 📱 **Создает инбокс для VK** (если его нет)
4. 🔗 **Настраивает вебхуки** в Chatwoot
5. 💾 **Сохраняет конфигурацию** в `.env` файл
6. 📋 **Показывает итоговую инструкцию** с готовыми URL

### Шаг 4: Запуск проекта

```bash
# Вернитесь в корень проекта
cd ../..

# Установите зависимости для VK коннектора
npm run install:vk

# Соберите и запустите VK коннектор
npm run build:vk && npm run start:vk
```

**Для разработки:** используйте `npm run dev:vk` вместо `npm run start:vk`

### Шаг 5: Настройка VK Callback API

После запуска скрипт покажет готовые URL для настройки:

1. Зайдите в [управление сообществом VK](https://vk.com/groups)
2. Перейдите в "Управление" → "Работа с API" → "Callback API"
3. Создайте сервер с параметрами:
   - **URL**: `https://your-domain.com/vk/callback/{VK_CALLBACK_ID}`
   - **Секретный ключ**: `{VK_SECRET}` (показан в результатах настройки)
   - **Строка подтверждения**: `{VK_CONFIRMATION}` (которую вы ввели)
   - **Версия API**: `5.199`
4. Включите типы событий: `message_new`, `message_reply`

### Для внешнего доступа (опционально)

```bash
# Создайте туннель через ngrok
cd apps/vk-connector
npm run auto-tunnel
```

Готово! 🎉 Проект настроен автоматически и готов к работе.

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ и npm
- Аккаунт ВКонтакте с созданным сообществом
- Установленный Chatwoot (можно использовать [облачную версию](https://www.chatwoot.com/))

### Шаг 1: Клонирование и установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd chatwoot-messenger-gateway

# Установите зависимости
npm install
```

### Шаг 2: Быстрая настройка VK коннектора

```bash
# Перейдите в директорию VK коннектора
cd apps/vk-connector

# Запустите интерактивную настройку
npm run setup
```

Откроется веб-интерфейс по адресу http://localhost:3000/setup.html

### Шаг 3: Запуск проекта

```bash
# Вернитесь в корень проекта
cd ../..

# Установите зависимости для VK коннектора
npm run install:vk

# Соберите и запустите VK коннектор
npm run build:vk && npm run start:vk
```

**Примечание:** Для разработки используйте `npm run dev:vk` вместо `npm run start:vk`

Готово! Проект запущен и готов к работе.

## 🔧 Подробная настройка

### 1. Получение кредов для ВКонтакте

#### Создание сообщества VK

1. Перейдите в [VK Управление сообществами](https://vk.com/groups)
2. Создайте новое сообщество или выберите существующее
3. В настройках сообщества перейдите в "Работа с API" → "Ключи доступа"

#### Получение токена доступа

1. В разделе "Ключи доступа" создайте новый ключ
2. Выберите права доступа:
   - **Сообщения** (для отправки и получения сообщений)
   - **Управление сообществом** (для получения информации о пользователях)
3. Сохраните ключ - это ваш `VK_ACCESS_TOKEN`

#### Настройка Callback API

1. В настройках сообщества перейдите в "Управление" → "Работа с API" → "Callback API"
2. Создайте сервер:
   - **Версия API**: `5.199`
   - **URL**: `https://your-domain.com/vk/callback/{VK_CALLBACK_ID}`
   - **Секретный ключ**: создайте уникальную строку (это ваш `VK_SECRET`)
   - **Строка подтверждения**: создайте уникальную строку (это ваш `VK_CONFIRMATION`)
3. Включите типы событий:
   - `message_new` (новые сообщения)
   - `message_reply` (ответы на сообщения)

#### Получение ID сообщества

ID вашего сообщества можно найти в URL страницы сообщества или через VK API.

### 2. Получение кредов для Chatwoot

#### Автоматически получаемые настройки

Некоторые настройки могут быть получены автоматически через API:

- **CHATWOOT_WEBHOOK_ID_VK** - генерируется автоматически при создании вебхука
- **VK_CALLBACK_ID** - можно использовать UUID или случайную строку
- **VK_SECRET** - может быть сгенерирован автоматически
- **VK_CONFIRMATION** - может быть сгенерирована автоматически

#### Создание инбокса

1. Войдите в вашу панель Chatwoot
2. Перейдите в "Settings" → "Inboxes"
3. Создайте новый инбокс типа "API"
4. После создания запишите `ID инбокса` (это ваш `VK_INBOX_ID`)

#### Получение API токена

1. В настройках профиля перейдите в "API Access"
2. Создайте новый токен доступа
3. Сохраните токен (это ваш `CHATWOOT_API_ACCESS_TOKEN`)

#### Получение ID аккаунта

ID аккаунта можно найти в URL вашей панели Chatwoot:
`https://your-chatwoot.com/app/accounts/{ACCOUNT_ID}/...`

#### Настройка вебхука

1. В настройках инбокса перейдите в "Integrations" → "Webhooks"
2. Создайте новый вебхук:
   - **Webhook URL**: `https://your-domain.com/chatwoot/webhook/{CHATWOOT_WEBHOOK_ID_VK}`
   - **События**: включите `message_created`
   - **Тип сообщений**: `outgoing`

## 🌐 Интерактивная настройка

### Использование веб-интерфейса

Проект предоставляет удобный веб-интерфейс для настройки:

```bash
cd apps/vk-connector
npm run setup
```

Откройте http://localhost:3000/setup.html в браузере:

1. **Заполните форму** всеми полученными кредами
2. **Сохраните конфигурацию** - кнопка "Save Configuration"
3. **Получите URL вебхуков** - кнопка "Get Webhook URLs"
4. **Создайте туннель** (для локальной разработки) - кнопка "Start Tunnel"

### Командная строка

Для настройки через командную строку:

```bash
# Интерактивная настройка
node scripts/setup.js

# Генерация webhook URL
npm run webhook

# Создание туннеля через ngrok
npm run tunnel
```

## 🛠 Команды для разработки

### Основные команды

```bash
# Установка всех зависимостей
npm install

# Запуск всех приложений в режиме разработки
npm run dev

# Сборка всех пакетов
npm run build

# Линтинг кода
npm run lint

# Запуск тестов
npm run test

# Очистка сборок
npm run clean
```

### VK Connector команды

```bash
# Установка зависимостей для VK коннектора
npm run install:vk

# Запуск VK коннектора в режиме разработки
npm run dev:vk

# Сборка VK коннектора
npm run build:vk

# Запуск VK коннектора
npm run start:vk

# Интерактивная настройка
cd apps/vk-connector && npm run setup

# Создание туннеля
cd apps/vk-connector && npm run tunnel

# Генерация webhook URL
cd apps/vk-connector && npm run webhook
```

### Разработка с Turborepo

```bash
# Запуск конкретного пакета
npx turbo run dev --filter=vk-connector

# Сборка конкретного пакета
npx turbo run build --filter=vk-connector

# Запуск тестов для конкретного пакета
npx turbo run test --filter=vk-connector
```

## ✅ Проверка работоспособности

### Health Check

Проверьте статус сервера:

```bash
curl http://localhost:3000/health
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "service": "vk-connector",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### Тестирование входящих сообщений

1. Напишите сообщение в группу ВКонтакте
2. Проверьте логи приложения - должно появиться:
   ```
   [vk] event received: type=message_new group_id=YOUR_GROUP_ID
   [chatwoot] create_message id=123 type=incoming
   ```
3. Убедитесь, что сообщение появилось в Chatwoot

### Тестирование исходящих сообщений

1. Ответьте на сообщение в Chatwoot
2. Проверьте логи приложения - должно появиться:
   ```
   [server] Sent message to VK: recipient=USER_ID content="Ваш ответ"
   [vk] SENT: peer_id=USER_ID message_id=456
   ```
3. Убедитесь, что ответ пришёл в ВКонтакте

### Проверка вебхуков

```bash
# Проверка VK вебхука
curl -X POST http://localhost:3000/vk/callback/YOUR_CALLBACK_ID \
  -H "Content-Type: application/json" \
  -d '{"type":"confirmation","group_id":YOUR_GROUP_ID}'

# Проверка Chatwoot вебхука
curl -X POST http://localhost:3000/chatwoot/webhook/YOUR_WEBHOOK_ID \
  -H "Content-Type: application/json" \
  -d '{"event":"message_created","message_type":"outgoing","content":"Test message"}'
```

## 🐛 Частые проблемы

### 1. Проблемы с зависимостями

**Ошибка:** `npm ERR! peer dep missing`

**Решение:**
```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Переустановите зависимости
npm install
```

### 2. Ошибки токенов VK

**Ошибка:** `VK API error 5: User authorization failed`

**Решение:**
- Проверьте правильность `VK_ACCESS_TOKEN`
- Убедитесь, что токен имеет необходимые права доступа
- Проверьте, что сообщество не заблокировано

### 3. Ошибки вебхуков VK

**Ошибка:** `Invalid secret` или `Invalid group_id`

**Решение:**
- Проверьте, что `VK_SECRET` в настройках VK и в `.env` файле совпадают
- Убедитесь, что `VK_GROUP_ID` указан правильно
- Проверьте, что URL вебхука доступен извне

### 4. Ошибки подключения к Chatwoot

**Ошибка:** `401 Unauthorized` или `404 Not Found`

**Решение:**
- Проверьте правильность `CHATWOOT_API_ACCESS_TOKEN`
- Убедитесь, что `CHATWOOT_BASE_URL` указан правильно (без слэша в конце)
- Проверьте, что `CHATWOOT_ACCOUNT_ID` существует

### 5. Порт уже используется

**Ошибка:** `Error: listen EADDRINUSE :::3000`

**Решение:**
```bash
# Измените порт в .env файле
PORT=3001

# Или остановите процесс, использующий порт
lsof -ti:3000 | xargs kill -9
```

### 6. Проблемы с ngrok

**Ошибка:** `Ngrok timeout` или `Ngrok process failed`

**Решение:**
```bash
# Установите ngrok глобально
npm install -g ngrok

# Проверьте установку
ngrok version

# Запустите вручную
ngrok http 3000
```

### 7. TypeScript ошибки

**Ошибка:** `Cannot find module` или TypeScript compilation errors

**Решение:**
```bash
# Сборка всех пакетов
npm run build

# Проверка типов
npx tsc --noEmit

# Очистка и пересборка
npm run clean && npm run build
```

### 8. Проблемы с CORS

**Ошибка:** CORS ошибки при доступе к веб-интерфейсу

**Решение:**
- Убедитесь, что вы обращаетесь к правильному порту
- Проверьте, что сервер запущен: `curl http://localhost:3000/health`
- При использовании обратного прокси настройте CORS заголовки

## 📞 Дополнительная помощь

Если у вас возникли проблемы:

1. Проверьте [README.md](README.md) для общей информации
2. Изучите [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) для технических деталей
3. Проверьте логи приложения для поиска ошибок
4. Создайте issue в репозитории с подробным описанием проблемы

## 🎉 Следующие шаги

После успешного запуска:

1. Настройте SSL-сертификаты для продакшн-окружения
2. Рассмотрите использование Docker для развертывания
3. Настройте мониторинг и логирование
4. Изучите возможность добавления других коннекторов мессенджеров

Удачной настройки! 🚀