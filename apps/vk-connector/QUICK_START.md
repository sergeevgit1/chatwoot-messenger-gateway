# Быстрый запуск VK Connector

Этот гид поможет вам быстро запустить и настроить VK коннектор.

## 🚀 Быстрый старт

### 1. 🚀 Улучшенная автоматическая настройка (рекомендуется)

Новый скрипт автоматической настройки сгенерирует все необходимые параметры и настроит Chatwoot:

```bash
cd apps/vk-connector
npm run auto-setup
```

Скрипт автоматически:
- ✨ Сгенерирует VK_CALLBACK_ID, VK_SECRET, VK_CONFIRMATION, CHATWOOT_WEBHOOK_ID_VK
- 🔍 Проверит подключение к Chatwoot
- 📱 Создаст инбокс для VK в Chatwoot
- 🔗 Настроит вебхуки
- 💾 Сохранит конфигурацию в .env файл
- 📋 Покажет готовую инструкцию по настройке

### 2. Ручная настройка

Если у вас уже есть все креды, создайте `.env` файл:

```bash
cp .env.example .env
# Отредактируйте .env файл с вашими данными
```

### 3. Получение webhook URL

Для локальной разработки:

```bash
# Запустите туннель
npm run auto-tunnel
```

### 4. Установка и запуск

```bash
# Установка зависимостей
npm run install:vk

# Запуск в режиме разработки
npm run dev:vk

# Или запуск продакшн версии
npm run build:vk && npm run start:vk
```

## 🌐 Настройка VK Callback API

После настройки перейдите в настройки вашей VK группы:

1. **URL Callback API**: `https://your-domain.com/vk/callback/{VK_CALLBACK_ID}`
2. **Секретный ключ**: `{VK_SECRET}`
3. **Строка подтверждения**: `{VK_CONFIRMATION}`
4. **События**: включите `message_new`

## 🤖 Настройка Chatwoot

1. **URL вебхука**: `https://your-domain.com/chatwoot/webhook/{CHATWOOT_WEBHOOK_ID_VK}`
2. **События**: включите `message_created`
3. **Тип сообщений**: `outgoing`

## 🧪 Тестирование

### Тест входящих сообщений

1. Напишите сообщение в группу ВКонтакте
2. Проверьте логи: должно появиться `[vk] adapter started`
3. Сообщение должно прийти в Chatwoot

### Тест исходящих сообщений

1. Ответьте на сообщение в Chatwoot
2. Проверьте логи: должно появиться `[server] Sent message to VK`
3. Ответ должен прийти в ВКонтакте

## 🔧 Health Check

Проверьте статус сервера:

```bash
curl http://localhost:3000/health
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "service": "vk-connector",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## 🐛 Поиск проблем

### Логи

Проверьте логи приложения:
```bash
# Просмотр логов в реальном времени
tail -f logs/vk-connector.log

# Или в Docker
docker logs vk-connector
```

### Общие проблемы

1. **Неверные креды** - проверьте `.env` файл
2. **Проблемы с webhook** - проверьте URL и настройки VK
3. **Проблемы с Chatwoot** - проверьте токен и URL
4. **Порт занят** - измените порт в `.env`

## 📋 Полезные команды

```bash
# Автоматическая настройка
npm run auto-setup

# Создание туннеля
npm run auto-tunnel

# Перезапуск с чистой сборкой
npm run clean && npm run build:vk && npm run start:vk
```

## 🎯 Следующие шаги

После успешного запуска:

1. Настройте автозапуск при перезагрузке сервера
2. Добавьте SSL сертификаты для продакшена
3. Настройте мониторинг и логирование
4. Рассмотрите Docker для продакшн развертывания

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте [README.md](README.md)
2. Изучите [docs/DEVELOPMENT.md](../docs/DEVELOPMENT.md)
3. Проверьте логи на ошибки
4. Создайте issue в репозитории

Удачной настройки! 🚀