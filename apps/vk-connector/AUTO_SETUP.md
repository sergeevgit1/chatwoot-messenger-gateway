# 🤖 Автоматическая настройка VK Connector

Подробное руководство по использованию системы автоматической настройки VK коннектора с умной генерацией параметров и интеграцией с Chatwoot API.

## 📋 Содержание

1. [Обзор возможностей](#обзор-возможностей)
2. [Команды автоматической настройки](#команды-автоматической-настройки)
3. [Процесс настройки](#процесс-настройки)
4. [Генерируемые параметры](#генерируемые-параметры)
5. [Интеграция с Chatwoot API](#интеграция-с-chatwoot-api)
6. [Веб-интерфейс](#веб-интерфейс)
7. [Устранение проблем](#устранение-проблем)

## 🌟 Обзор возможностей

Система автоматической настройки включает:

- 🤖 **Умный скрипт настройки** - автоматически генерирует все технические параметры
- 🔗 **Интеграция с Chatwoot API** - создает инбоксы и вебхуки автоматически
- 🌐 **Веб-интерфейс** - пошаговый мастер с визуальным прогрессом
- ✨ **Валидация параметров** - проверка корректности всех вводимых данных
- 🚇 **Интеграция с ngrok** - создание туннелей для локальной разработки
- 💾 **Автоматическое сохранение** - конфигурация сохраняется в `.env` файл

## 🚀 Команды автоматической настройки

### Основная команда

```bash
# Запуск автоматической настройки в консоли
npm run auto-setup
```

### Дополнительные команды

```bash
# Создание туннеля для вебхуков
npm run auto-tunnel

# Запуск веб-интерфейса
npm run setup

# Тестирование подключения к Chatwoot
npm run test-chatwoot
```

## 📝 Процесс настройки

### Шаг 1: Запуск скрипта

```bash
cd apps/vk-connector
npm run auto-setup
```

### Шаг 2: Ввод необходимых параметров

Скрипт пошагово запросит следующие параметры:

| Параметр | Описание | Пример |
|----------|----------|--------|
| Chatwoot Base URL | URL вашего Chatwoot сервера | `https://chatwoot.example.com` |
| Chatwoot API Access Token | Токен доступа к API | `cGxhY2Vob2xkZXItdG9rZW4=` |
| Chatwoot Account ID | ID аккаунта Chatwoot | `1` |
| VK Access Token | Токен доступа сообщества VK | `vk1.a.qwerty...` |
| VK Group ID | ID сообщества ВКонтакте | `12345678` |
| VK Confirmation String | Строка подтверждения из настроек VK | `my-confirmation-string` |
| Server Port | Порт сервера (опционально) | `3000` |

### Шаг 3: Автоматические операции

Скрипт автоматически выполнит следующие действия:

1. ✨ **Генерация параметров**
   - `VK_CALLBACK_ID` - UUID v4 для идентификации callback
   - `VK_SECRET` - случайная строка (32 символа) для безопасности
   - `CHATWOOT_WEBHOOK_ID_VK` - UUID v4 для вебхука
   - `VK_API_VERSION` - версия VK API (5.199)
   - `HOST` - хост сервера (0.0.0.0)

2. 🔍 **Проверка подключения к Chatwoot**
   - Тестирование соединения с сервером
   - Валидация API токена
   - Проверка доступа к аккаунту

3. 📱 **Работа с инбоксами**
   - Поиск существующего VK инбокса
   - Создание нового инбокса при необходимости
   - Получение ID инбокса

4. 🔗 **Настройка вебхуков**
   - Создание вебхука в Chatwoot
   - Генерация URL для VK Callback API
   - Настройка подписок на события

5. 💾 **Сохранение конфигурации**
   - Запись всех параметров в `.env` файл
   - Сортировка параметров по алфавиту
   - Проверка корректности сохранения

### Шаг 4: Итоговая инструкция

После завершения настройки скрипт покажет:

```
🎉 Настройка завершена успешно!

📋 Следующие шаги:

1. 🚀 Запустите коннектор:
   npm run install:vk && npm run start:vk

2. 🔧 Настройте VK Callback API:
   URL: http://localhost:3000/vk/callback/12345678-1234-1234-1234-123456789012
   Секретный ключ: aBcDeFgHiJkLmNoPqRsTuVwXyZ123456
   Строка подтверждения: my-confirmation-string
   Версия API: 5.199

3. 🌐 Для внешнего доступа (если необходимо):
   npm run tunnel

4. 📡 После запуска туннеля обновите URL в настройках VK:
   Используйте URL от ngrok вместо localhost

5. ✅ Проверьте работу:
   Отправьте тестовое сообщение в группу VK

🔗 Готовые URL для вебхуков:
   VK Callback: http://localhost:3000/vk/callback/12345678-1234-1234-1234-123456789012
   Chatwoot: http://localhost:3000/chatwoot/webhook/87654321-4321-4321-4321-210987654321
```

## 🔧 Генерируемые параметры

### VK_CALLBACK_ID
```javascript
function generateUUID() {
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
    const r = Math.random() * 16 | 0;
    const v = c === 'x' ? r : (r & 0x3 | 0x8);
    return v.toString(16);
  });
}
```
- **Формат**: UUID v4
- **Пример**: `12345678-1234-1234-1234-123456789012`
- **Использование**: Идентификатор для VK Callback API

### VK_SECRET
```javascript
function generateRandomString(length) {
  const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
  let result = '';
  for (let i = 0; i < length; i++) {
    result += chars.charAt(Math.floor(Math.random() * chars.length));
  }
  return result;
}
```
- **Формат**: Случайная строка
- **Длина**: 32 символа
- **Использование**: Секретный ключ для VK Callback API

### CHATWOOT_WEBHOOK_ID_VK
- **Формат**: UUID v4
- **Пример**: `87654321-4321-4321-4321-210987654321`
- **Использование**: Идентификатор для вебхука Chatwoot

## 🔗 Интеграция с Chatwoot API

### Класс ChatwootAPI

Скрипт использует специальный класс для работы с Chatwoot API:

```javascript
class ChatwootAPI {
  constructor(baseUrl, apiToken, accountId) {
    this.baseUrl = baseUrl.replace(/\/$/, '');
    this.accountId = accountId;
    this.apiToken = apiToken;
    this.headers = {
      'Content-Type': 'application/json',
      'api_access_token': apiToken,
      'Authorization': `Bearer ${apiToken}`,
    };
  }
}
```

### Методы API

#### `testConnection()`
- Проверяет подключение к Chatwoot
- Валидирует токен доступа
- Проверяет доступ к аккаунту

#### `getInboxes()`
- Получает список всех инбоксов
- Ищет существующий VK инбокс
- Возвращает массив инбоксов

#### `createVKInbox()`
- Создает новый инбокс для VK
- Устанавливает канал типа "API"
- Возвращает данные созданного инбокса

#### `createWebhook(inboxId, webhookUrl)`
- Создает вебхук для указанного инбокса
- Настраивает подписки на события
- Возвращает данные созданного вебхука

## 🌐 Веб-интерфейс

### Запуск веб-интерфейса

```bash
cd apps/vk-connector
npm run setup
```

Откройте http://localhost:3000/setup.html в браузере.

### Режимы работы

#### 🤖 Автоматический режим
- Пошаговый мастер с 4 шагами
- Визуальный прогресс-бар
- Проверка подключения к Chatwoot
- Автоматическая генерация параметров

#### ⚙️ Ручной режим
- Полный контроль над всеми параметрами
- Ввод всех конфигурационных значений
- Генерация URL вебхуков
- Создание туннелей

### API эндпоинты веб-интерфейса

#### `/api/test-chatwoot`
```javascript
POST /api/test-chatwoot
Content-Type: application/json

{
  "baseUrl": "https://chatwoot.example.com",
  "apiToken": "token",
  "accountId": "1"
}
```

#### `/api/auto-setup`
```javascript
POST /api/auto-setup
Content-Type: application/json

{
  "chatwoot_base_url": "https://chatwoot.example.com",
  "chatwoot_api_access_token": "token",
  "chatwoot_account_id": "1",
  "vk_access_token": "vk1.a.token",
  "vk_group_id": "12345678",
  "vk_confirmation": "confirmation-string",
  "port": "3000"
}
```

#### `/api/tunnel`
```javascript
POST /api/tunnel
Content-Type: application/json

{
  "port": "3000"
}
```

## 🚇 Работа с туннелями

### Создание туннеля через ngrok

```bash
npm run auto-tunnel
```

Или через скрипт:
```bash
node scripts/auto-setup.js --tunnel
```

### Автоматическое обновление URL

После создания туннеля скрипт покажет обновленные URL:

```
📋 Обновленные URL для настройки:

VK Callback API:
   URL: https://abc123.ngrok.io/vk/callback/12345678-1234-1234-1234-123456789012
   Секрет: aBcDeFgHiJkLmNoPqRsTuVwXyZ123456
   Подтверждение: my-confirmation-string

Chatwoot Webhook:
   URL: https://abc123.ngrok.io/chatwoot/webhook/87654321-4321-4321-4321-210987654321
```

## 🔍 Валидация параметров

### Валидаторы

#### `validateURL(url)`
- Проверяет корректность URL
- Использует конструктор URL()
- Возвращает `true`/`false`

#### `validateNumber(value)`
- Проверяет, что значение является числом
- Проверяет, что число положительное
- Возвращает `true`/`false`

#### `validateVKToken(token)`
- Проверяет формат токена VK
- Ожидает префикс `vk1.`
- Проверяет минимальную длину

### Примеры валидации

```javascript
// Корректные значения
validateURL('https://chatwoot.example.com') // true
validateNumber('12345') // true
validateVKToken('vk1.a.qwerty123456') // true

// Некорректные значения
validateURL('invalid-url') // false
validateNumber('-123') // false
validateVKToken('invalid-token') // false
```

## 🐛 Устранение проблем

### Ошибка `fetch is not a function`

**Проблема**: `ReferenceError: fetch is not a function`

**Описание**: Эта ошибка возникает в Node.js среде, где `fetch` не доступен по умолчанию в версиях до 18.

**Решение**: Скрипт автоматически определяет версию Node.js и использует соответствующую реализацию:

- **Node.js 18+**: Используется встроенный `fetch`
- **Node.js < 18**: Используется `node-fetch` версии 2.x (CommonJS совместимая)

**Обновленные зависимости**:
```json
{
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.2"
  }
}
```

**Отладочная информация**:
```
📌 Используем встроенный fetch (Node.js v25.2.1)
```

### Ошибка подключения к Chatwoot

**Проблема**: `Ошибка подключения к Chatwoot: HTTP 401: Unauthorized`

**Решение**:
1. Проверьте правильность API токена
2. Убедитесь, что токен имеет необходимые права
3. Проверьте URL Chatwoot сервера

### Улучшенная обработка сетевых ошибок

Скрипт теперь предоставляет детальную информацию о сетевых ошибках:

- **ENOTFOUND**: Не удалось разрешить имя хоста
- **ECONNREFUSED**: Соединение отклонено (сервер недоступен)
- **ETIMEDOUT**: Таймаут подключения

**Пример вывода при ошибке**:
```
❌ Ошибка подключения к Chatwoot: Не удалось разрешить имя хоста. Проверьте URL: https://invalid-url.com
```

### Ошибка создания инбокса

**Проблема**: `Ошибка создания инбокса: HTTP 403: Forbidden`

**Решение**:
1. Проверьте права доступа токена
2. Убедитесь, что аккаунт существует
3. Проверьте, что токен имеет права на создание инбоксов

### Ошибка токена VK

**Проблема**: `Некорректный формат токена VK`

**Решение**:
1. Убедитесь, что токен начинается с `vk1.`
2. Проверьте, что токен получен для сообщества
3. Проверьте, что токен имеет права на сообщения

### Проблемы с ngrok

**Проблема**: `Ngrok timeout` или `Ngrok process failed`

**Решение**:
```bash
# Установите ngrok глобально
npm install -g ngrok

# Проверьте установку
ngrok version

# Запустите вручную
ngrok http 3000
```

### Ошибка сохранения .env файла

**Проблема**: `Ошибка при сохранении файла .env: EACCES: permission denied`

**Решение**:
1. Проверьте права доступа к директории
2. Убедитесь, что процесс имеет права на запись
3. Проверьте, что файл не заблокирован другим процессом

## 📚 Дополнительная информация

### Структура .env файла

```bash
# Chatwoot Configuration
CHATWOOT_ACCOUNT_ID=1
CHATWOOT_API_ACCESS_TOKEN=your-api-token
CHATWOOT_BASE_URL=https://chatwoot.example.com
CHATWOOT_WEBHOOK_ID_VK=87654321-4321-4321-4321-210987654321

# VK Configuration
VK_ACCESS_TOKEN=vk1.a.your-vk-token
VK_GROUP_ID=12345678
VK_CALLBACK_ID=12345678-1234-1234-1234-123456789012
VK_SECRET=aBcDeFgHiJkLmNoPqRsTuVwXyZ123456
VK_CONFIRMATION=my-confirmation-string
VK_API_VERSION=5.199
VK_INBOX_ID=1

# Server Configuration
HOST=0.0.0.0
PORT=3000
```

### Логирование

Скрипт предоставляет подробное логирование:

- ✅ **Успешные операции** - зеленые маркеры
- ❌ **Ошибки** - красные маркеры с описанием
- 🔍 **Информационные сообщения** - синие маркеры
- 📋 **Результаты операций** - желтые маркеры

### Полезные команды

```bash
# Проверка синтаксиса скрипта
node -c scripts/auto-setup.js

# Запуск с отладкой
DEBUG=* npm run auto-setup

# Просмотр сгенерированной конфигурации
cat .env

# Тестирование подключения к Chatwoot
curl -H "api_access_token: your-token" \
     https://chatwoot.example.com/api/v1/accounts/1/inboxes
```

---

🎉 **Готово!** Теперь вы знаете всё об автоматической настройке VK коннектора.

Если у вас остались вопросы, проверьте [QUICK_START.md](QUICK_START.md) или создайте issue в репозитории.