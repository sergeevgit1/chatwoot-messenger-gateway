# 🚀 Развертывание Chatwoot Messenger Gateway через Dokploy

Это руководство подробно описывает процесс развертывания проекта Chatwoot Messenger Gateway с использованием Dokploy и Traefik.

## 📋 Содержание

1. [Подготовка репозитория](#подготовка-репозитория)
2. [Настройка Dokploy](#настройка-dokploy)
3. [Создание приложения в Dokploy](#создание-приложения-в-dokploy)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Конфигурация доменов и SSL](#конфигурация-доменов-и-ssl)
6. [Настройка вебхуков с Traefik](#настройка-вебхуков-с-traefik)
7. [Проверка работоспособности](#проверка-работоспособности)
8. [Мониторинг и логирование](#мониторинг-и-логирование)
9. [Решение типичных проблем](#решение-типичных-проблем)

## 📋 Требования

- Установленный Dokploy с Traefik
- Настроенный домен с доступом к серверу
- Репозиторий с кодом проекта (GitHub, GitLab, Bitbucket)
- Существующий инстанс Chatwoot с доступом к API

## 🛠️ Подготовка репозитория

### 1. Убедитесь, что все необходимые файлы присутствуют

Ваш репозиторий должен содержать:
- [`docker-compose.yml`](docker-compose.yml:1) - конфигурация Docker
- [`apps/vk-connector/Dockerfile`](apps/vk-connector/Dockerfile:1) - Dockerfile для VK коннектора
- [`.env.example`](.env.example:1) - пример переменных окружения

### 2. Настройте переменные окружения

Скопируйте файл `.env.example` в `.env` и настройте основные параметры:

```bash
cp .env.example .env
```

Обязательно настройте следующие параметры:

```bash
# VK Configuration
VK_GROUP_ID=12345678                    # ID вашего VK сообщества
VK_ACCESS_TOKEN=vk1.a.abc123...         # Токен доступа VK API
VK_SECRET=your_secret_key_here          # Секретный ключ для вебхуков
VK_CONFIRMATION=your_confirmation       # Строка подтверждения из VK API

# Chatwoot Configuration
CHATWOOT_API_ACCESS_TOKEN=your_token    # API токен Chatwoot
CHATWOOT_ACCOUNT_ID=1                   # ID аккаунта в Chatwoot
CHATWOOT_BASE_URL=https://chatwoot.yourdomain.com  # URL вашего Chatwoot

# Server Configuration
PORT=3000                               # Порт приложения
HOST=0.0.0.0                            # Хост для прослушивания
```

## 🚀 Настройка Dokploy

### 1. Установка Traefik с Dokploy

Если вы еще не настроили Traefik, выполните следующие шаги:

1. В интерфейсе Dokploy перейдите в раздел "Settings"
2. Установите Traefik, следуя инструкциям Dokploy
3. Убедитесь, что Traefik настроен на автоматическое получение SSL-сертификатов

### 2. Подготовка к созданию приложения

Убедитесь, что у вас есть:
- Доступ к репозиторию с проектом
- Права на создание приложений в Dokploy
- Настроенный домен для приложения

## 📦 Создание приложения в Dokploy

### 1. Создание нового приложения

1. В интерфейсе Dokploy нажмите "New Application"
2. Выберите "Docker Compose" как тип приложения
3. Укажите имя приложения (например: `vk-connector`)
4. Подключите репозиторий с кодом проекта

### 2. Настройка сборки

В настройках приложения укажите:

- **Build Context**: `.` (корень репозитория)
- **Docker Compose Path**: `docker-compose.yml`
- **Auto-deploy**: Включите для автоматического развертывания при пуше в основную ветку

### 3. Настройка портов

Убедитесь, что порт 3000 правильно настроен:
- **Container Port**: `3000`
- **Protocol**: `HTTP`
- **Expose**: `true`

## ⚙️ Настройка переменных окружения

### 1. Основные переменные

В интерфейсе Dokploy перейдите в раздел "Environment Variables" и добавьте:

```bash
# VK Configuration
VK_GROUP_ID=12345678
VK_ACCESS_TOKEN=vk1.a.abc123...
VK_SECRET=your_secret_key_here
VK_CONFIRMATION=your_confirmation_string
VK_API_VERSION=5.199

# Chatwoot Configuration
CHATWOOT_API_ACCESS_TOKEN=your_chatwoot_token
CHATWOOT_ACCOUNT_ID=1
CHATWOOT_BASE_URL=https://chatwoot.yourdomain.com

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production
```

### 2. Генерация недостающих параметров

Используйте встроенный скрипт для генерации недостающих параметров:

```bash
# Генерация VK_CALLBACK_ID
VK_CALLBACK_ID=$(openssl rand -hex 16)

# Генерация CHATWOOT_WEBHOOK_ID_VK
CHATWOOT_WEBHOOK_ID_VK=$(openssl rand -hex 32)
```

### 3. Проверка переменных окружения

После настройки переменных окружения, убедитесь, что все обязательные параметры заполнены. Приложение не запустится без следующих переменных:

- `VK_GROUP_ID`
- `VK_ACCESS_TOKEN`
- `VK_SECRET`
- `VK_CONFIRMATION`
- `CHATWOOT_API_ACCESS_TOKEN`
- `CHATWOOT_ACCOUNT_ID`
- `CHATWOOT_BASE_URL`

## 🌐 Конфигурация доменов и SSL

### 1. Настройка домена

1. В интерфейсе Dokploy перейдите в раздел "Domains"
2. Добавьте домен для вашего приложения (например: `vk-connector.yourdomain.com`)
3. Убедитесь, что DNS-запись A или CNAME указывает на ваш сервер

### 2. Автоматический SSL

Traefik автоматически настроит SSL-сертификат для вашего домена:
1. Убедитесь, что домен доступен и указывает на ваш сервер
2. Traefik автоматически запросит SSL-сертификат Let's Encrypt
3. Проверьте, что сайт доступен по HTTPS

### 3. Проверка домена

После настройки домена проверьте:
- Доступность сайта по HTTP
- Автоматический редирект на HTTPS
- Корректность SSL-сертификата

## 🔗 Настройка вебхуков с Traefik

### 1. VK Callback API

После развертывания приложения вам нужно настроить Callback API в VK:

1. Получите URL вебхука: `https://vk-connector.yourdomain.com/vk/callback/{VK_CALLBACK_ID}`
2. Перейдите в настройки сообщества VK → "Управление" → "Работа с API"
3. В настройках Callback API укажите полученный URL
4. В поле "Строка подтверждения" укажите значение из переменной `VK_CONFIRMATION`

### 2. Chatwoot Webhook

Настройте вебхук в Chatwoot для отправки сообщений:

1. Войдите в админ-панель Chatwoot
2. Перейдите в "Settings" → "Integrations" → "Webhooks"
3. Создайте новый вебхук с URL: `https://vk-connector.yourdomain.com/chatwoot/webhook/{CHATWOOT_WEBHOOK_ID_VK}`
4. Выберите события: "Message Created"
5. Укажите типы сообщений: "Outgoing"

### 3. Проверка вебхуков

Проверьте работу вебхуков:
1. Отправьте тестовое сообщение из VK
2. Проверьте, что сообщение появилось в Chatwoot
3. Отправьте ответное сообщение из Chatwoot
4. Проверьте, что сообщение доставлено в VK

## 🔍 Проверка работоспособности

### 1. Health Check

Приложение предоставляет эндпоинт для проверки работоспособности:

```bash
curl https://vk-connector.yourdomain.com/health
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "service": "vk-connector",
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

### 2. Логи приложения

Проверьте логи приложения в интерфейсе Dokploy:
1. Перейдите в раздел "Logs"
2. Ищите сообщения об ошибках или предупреждения
3. Убедитесь, что приложение успешно запустилось

### 3. Проверка интеграции

Выполните полную проверку интеграции:
1. Отправьте сообщение в VK сообщество
2. Проверьте, что сообщение появилось в Chatwoot
3. Ответьте на сообщение в Chatwoot
4. Убедитесь, что ответ доставлен в VK

## 📊 Мониторинг и логирование

### 1. Мониторинг в Dokploy

Dokploy предоставляет базовый мониторинг:
- **Resource Usage**: использование CPU и памяти
- **Health Status**: статус работоспособности приложения
- **Deployment History**: история развертываний

### 2. Настройка логов

Для улучшения логирования добавьте следующие переменные окружения:

```bash
# Log Configuration
LOG_LEVEL=info                    # Уровень логирования (debug, info, warn, error)
NODE_ENV=production               # Режим работы приложения
```

### 3. Внешний мониторинг (опционально)

Для расширенного мониторинга можно интегрировать:
- **Uptime Robot** для мониторинга доступности
- **Grafana + Prometheus** для метрик
- **Sentry** для отслеживания ошибок

## 🛠️ Решение типичных проблем

### 1. Приложение не запускается

**Проблема**: Приложение не запускается или падает с ошибкой.

**Решение**:
1. Проверьте логи в интерфейсе Dokploy
2. Убедитесь, что все обязательные переменные окружения настроены
3. Проверьте корректность токенов доступа VK и Chatwoot

```bash
# Проверка переменных окружения
echo $VK_GROUP_ID
echo $VK_ACCESS_TOKEN
echo $CHATWOOT_API_ACCESS_TOKEN
```

### 2. Проблемы с SSL

**Проблема**: SSL-сертификат не устанавливается или сайт недоступен по HTTPS.

**Решение**:
1. Проверьте, что домен указывает на ваш сервер
2. Убедитесь, что порт 80 и 443 доступны
3. Проверьте логи Traefik на наличие ошибок Let's Encrypt

```bash
# Проверка DNS
dig vk-connector.yourdomain.com

# Проверка доступности портов
telnet your-server-ip 80
telnet your-server-ip 443
```

### 3. Вебхуки не работают

**Проблема**: Сообщения не доставляются между VK и Chatwoot.

**Решение**:
1. Проверьте URL вебхуков в настройках VK и Chatwoot
2. Убедитесь, что `VK_CALLBACK_ID` и `CHATWOOT_WEBHOOK_ID_VK` совпадают
3. Проверьте логи на наличие ошибок при обработке вебхуков

### 4. Ошибка аутентификации в VK

**Проблема**: VK возвращает ошибку аутентификации.

**Решение**:
1. Проверьте, что `VK_ACCESS_TOKEN` действителен и имеет необходимые права
2. Убедитесь, что `VK_SECRET` совпадает с настройками в Callback API VK
3. Проверьте, что `VK_CONFIRMATION` совпадает с настройками в VK

### 5. Ошибка подключения к Chatwoot

**Проблема**: Приложение не может подключиться к Chatwoot API.

**Решение**:
1. Проверьте, что `CHATWOOT_BASE_URL` доступен из приложения
2. Убедитесь, что `CHATWOOT_API_ACCESS_TOKEN` имеет необходимые права
3. Проверьте, что `CHATWOOT_ACCOUNT_ID` корректен

```bash
# Проверка подключения к Chatwoot
curl -H "Authorization: Bearer $CHATWOOT_API_ACCESS_TOKEN" \
     "$CHATWOOT_BASE_URL/api/v1/accounts/$CHATWOOT_ACCOUNT_ID"
```

### 6. Проблемы с памятью или CPU

**Проблема**: Приложение потребляет слишком много ресурсов.

**Решение**:
1. Проверьте логи на наличие утечек памяти
2. Настройте ограничения ресурсов в Docker Compose
3. Рассмотрите возможность масштабирования приложения

### 7. Проблемы с обновлением

**Проблема**: Новая версия приложения не развертывается.

**Решение**:
1. Проверьте, что изменения pushed в репозиторий
2. Убедитесь, что auto-deploy включен
3. Проверьте логи сборки на наличие ошибок

## 🔄 Обновление приложения

Для обновления приложения до новой версии:

1. Внесите изменения в код
2. Запушьте изменения в репозиторий
3. Dokploy автоматически соберет и развернет новую версию
4. Проверьте работоспособность после обновления

## 📞 Поддержка

Если у вас возникли проблемы с развертыванием:

1. Проверьте логи приложения в Dokploy
2. Ознакомьтесь с [документацией проекта](README.md)
3. Изучите [руководство по быстрому старту](QUICK_START.md)
4. Проверьте [автоматическую настройку](apps/vk-connector/AUTO_SETUP.md)

---

**Примечание**: Это руководство предполагает, что у вас уже есть настроенный инстанс Chatwoot и доступ к VK API с необходимыми правами.